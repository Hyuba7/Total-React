import React from 'react';
import {render} from 'react-dom'

function FruitLopps() {
  let fruit:Array<string> = [
    "red",
    "blue",
    "yellow",
    "green"
  ]

return (
  <ul>
    {fruit.map((value,index) => {
      return
    }
    )}
  </ul>
)

}